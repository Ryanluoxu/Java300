package io.ryanluoxu.thread.basic;

public class Introduction {
	
	/**
	 * 程序：program. collection of codes. static.
	 * 进程：one process is for running one program. dynamic. (CPU,data,codes)
	 * 线程：in process, we can have many threads. to do different tasks at the same time.
	 * 	- 共享同一份资源（代码和数据）。有可能引起并发的问题
	 * 	- 多线程是模拟出来的多条路劲，事实上还是一个CPU,靠调度实现多线程。在线程之间来回切换。
	 * 		比如：
	 * 		- main()
	 * 		- gc
	 * 		- exception
	 */

}
